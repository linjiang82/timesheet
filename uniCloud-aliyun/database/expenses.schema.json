{
  "bsonType": "object",
  "permission": {
    "read": "auth.uid == doc.user_id || 'manager' in auth.role",
    "create": "auth.uid != null",
    "update": "auth.uid == doc.user_id || 'manager' in auth.role",
    "delete": "auth.uid == doc.user_id && doc.status == 'pending'"
  },
  "properties": {
    "_id": {
      "description": "ID"
    },
    "user_id": {
      "bsonType": "string",
      "description": "User ID",
      "foreignKey": "users._id"
    },
    "date": {
      "bsonType": "string",
      "description": "Date of expense (YYYY-MM-DD)"
    },
    "amount": {
      "bsonType": "double",
      "description": "Expense amount"
    },
    "category": {
      "bsonType": "string",
      "description": "Expense category (e.g., travel, meal, supplies)"
    },
    "receipt_image_url": {
      "bsonType": "string",
      "description": "URL of the receipt image in uniCloud storage"
    },
    "description": {
      "bsonType": "string",
      "description": "Brief description of the expense"
    },
    "status": {
      "bsonType": "string",
      "description": "Approval status",
      "enum": ["pending", "approved", "rejected"],
      "defaultValue": "pending"
    },
    "manager_comment": {
      "bsonType": "string",
      "description": "Comment from manager"
    },
    "created_at": {
      "bsonType": "timestamp",
      "description": "Created at"
    },
    "updated_at": {
      "bsonType": "timestamp",
      "description": "Updated at"
    }
  }
}
